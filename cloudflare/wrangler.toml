name = "jubuddy-poker"
main = "poker-worker.js"
compatibility_date = "2026-09-01"

[vars]
POKER_ORIGIN = "jub-poker.fly.dev"

# After first deploy, attach a route in the Cloudflare dashboard:
#   jubuddy.com/poker*
# Do not replace the zone apex — jubuddy.com/ still goes to the existing Vercel site.
